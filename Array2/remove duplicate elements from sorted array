class Solution {
    public int removeDuplicates(int[] nums) {
        int a=nums.length;
        int i,j,k;
        for(i=1,k=1; i<nums.length ;i++){
            if(nums[i]!=nums[i-1]){
                nums[k++]=nums[i];
            }
        }
        
        return k;
    }
}
